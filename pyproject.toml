[project]
name = "gh-runner"
version = "0.1.0"
description = "GitHub Actions self-hosted runner bridge for Jenkins"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "aiohttp>=3.14.1",
]
